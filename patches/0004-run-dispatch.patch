diff --git a/gateway/run.py b/gateway/run.py
index b071e48..48c8e6a 100644
--- a/gateway/run.py
+++ b/gateway/run.py
@@ -10940,6 +10940,9 @@ class GatewayRunner(GatewayAuthorizationMixin, GatewayKanbanWatchersMixin, Gatew
         if canonical == "compress":
             return await self._handle_compress_command(event)
 
+        if canonical == "saved":
+            return await self._handle_saved_command(event)
+
         if canonical == "usage":
             return await self._handle_usage_command(event)
 
